/**
 * @type {import('../../dist/umd/nacl')}
 */
var nacl;
/**
 * @type {Chai.ExpectStatic}
 */
var expect;
/**
 * @type {(s: string) => Uint8Array}
 */
var decodeBase64;
/**
 * @type {{ }[]}
 */
var hashData;
/**
 * @type {{ }[]}
 */
var hashTestVectors;

describe('nacl.hash.hash', function () {
    nacl.hash.hash // TODO
});